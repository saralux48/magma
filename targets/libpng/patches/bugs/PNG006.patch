diff --git a/png.c b/png.c
index e92008d..8d3cecb 100644
--- a/png.c
+++ b/png.c
@@ -616,6 +616,9 @@ png_free_data(png_const_structrp png_ptr, png_inforp info_ptr, png_uint_32 mask,
 #endif
 
 #ifdef PNG_eXIf_SUPPORTED
+#ifdef MAGMA_ENABLE_CANARIES
+   MAGMA_LOG("%MAGMA_BUG%", MAGMA_AND(info_ptr->eXIf_buf != NULL, (mask & info_ptr->free_me & PNG_FREE_EXIF) == 0));
+#endif
    /* Free any eXIf entry */
    if (((mask & PNG_FREE_EXIF) & info_ptr->free_me) != 0)
    {
diff --git a/pngset.c b/pngset.c
index 3e63c2724..e73a670a6 100644
--- a/pngset.c
+++ b/pngset.c
@@ -350,7 +350,9 @@ png_set_eXIf_1(png_const_structrp png_ptr, png_inforp info_ptr,
 
    info_ptr->num_exif = num_exif;
    info_ptr->exif = new_exif;
+#ifdef MAGMA_ENABLE_FIXES
    info_ptr->free_me |= PNG_FREE_EXIF;
+#endif
    info_ptr->valid |= PNG_INFO_eXIf;
 }
 #endif /* eXIf */
